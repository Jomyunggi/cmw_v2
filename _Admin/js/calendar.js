$(document).ready(function(){
	$('#ip_startDate1').datepicker({
		changeMonth: true,
		dateFormat : 'yy-mm-dd',
		showAnim : 'slideDown',
		onClose: function( selectedDate ) {
			$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
		}
	});
	$('#ip_endDate1').datepicker({
		changeMonth: true,
		dateFormat : 'yy-mm-dd',
		showAnim : 'slideDown',
		onClose: function( selectedDate ) {
			$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
		}
	});
	$('#loadBalancer_startDate1').datepicker({
		changeMonth: true,
		dateFormat : 'yy-mm-dd',
		showAnim : 'slideDown',
		onClose: function( selectedDate ) {
			$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
		}
	});
	$('#loadBalancer_endDate1').datepicker({
		changeMonth: true,
		dateFormat : 'yy-mm-dd',
		showAnim : 'slideDown',
		onClose: function( selectedDate ) {
			$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
		}
	});
	$('#loadBalancer_endDate').datepicker({
		changeMonth: true,
		dateFormat : 'yy-mm-dd',
		showAnim : 'slideDown',
		onClose: function( selectedDate ) {
			$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
		}
	});
});

function calender(){
	$(function() {
		$('#ip_startDate2').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate2').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_startDate3').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate3').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_startDate4').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate4').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_startDate5').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate5').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_startDate6').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate6').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_startDate7').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate7').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_startDate8').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate8').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_startDate9').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate9').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_startDate10').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#ip_endDate10').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});

		$('#loadBalancer_startDate2').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#loadBalancer_endDate2').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#loadBalancer_startDate3').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
		$('#loadBalancer_endDate3').datepicker({
			changeMonth: true,
			dateFormat : 'yy-mm-dd',
			showAnim : 'slideDown',
			onClose: function( selectedDate ) {
				$( '#endDate' ).datepicker( 'option', 'minDate', selectedDate );
			}
		});
	});
}